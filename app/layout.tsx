import type { Metadata } from "next";
import { Inter, Playfair_Display } from "next/font/google";
import "./globals.css";
import { Header } from "@/components/Header";
import { Footer } from "@/components/Footer";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

const playfair = Playfair_Display({
  subsets: ["latin"],
  variable: "--font-playfair",
  display: "swap",
});

export const metadata: Metadata = {
  metadataBase: new URL("https://legalintakeflow.com"),
  title: {
    default: "Legal Intake Flow — AI Intake Systems for Disability Attorneys",
    template: "%s | Legal Intake Flow",
  },
  description:
    "Legal Intake Flow connects disability attorneys and advocates with high-intent individuals actively preparing for SSDI and SSI benefits claims. Smarter intake. Better cases. More results.",
  openGraph: {
    type: "website",
    siteName: "Legal Intake Flow",
    locale: "en_US",
  },
  robots: {
    index: true,
    follow: true,
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={`${inter.variable} ${playfair.variable}`}>
      <body className="flex min-h-screen flex-col font-sans antialiased">
        <Header />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
